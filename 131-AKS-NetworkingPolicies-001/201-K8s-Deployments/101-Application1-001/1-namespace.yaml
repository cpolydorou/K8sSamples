###################################################################################################################
# The deployment of the nginx namespace
###################################################################################################################

{
  "apiVersion": "v1",
  "kind": "Namespace",
  "metadata": {
    "name": "app-1",
    "labels": {
      "name": "app-1",
      "env": "prod"
    }
  }
}