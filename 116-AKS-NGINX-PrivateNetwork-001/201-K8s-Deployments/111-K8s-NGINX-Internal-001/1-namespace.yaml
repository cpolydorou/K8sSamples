###################################################################################################################
# The deployment of the nginx namespace
###################################################################################################################

{
  "apiVersion": "v1",
  "kind": "Namespace",
  "metadata": {
    "name": "nginx-internal",
    "labels": {
      "name": "nginx-internal"
    }
  }
}